/* eslint-disable max-len */
import React from 'react';

const AddBook = () => (
  <form>
    <input name="title" placeholder="Add a title book" />
    <input name="author" placeholder="Add a name of author" />

    <button type="submit">ADD BOOK</button>
  </form>
);

export default AddBook;
